server:
  port: 8081

spring:
    cloud:
      config:
        server:
          git:
            uri: //URL REPO
            searchPaths: config-data
            username: ${GIT_USER}
            password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: secrete