server:
    port: 8081




spring:
    application:
      name: cloud-server
    cloud:
      config:
        uri: http://localhost:8080
        import: configserver:http://localhost:8080
        server:
          git:
            uri: https://github.com/ARNIXMINADEO/java-practice/config-data
            default-label: devel
            username: ${GIT_USER}
            password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: secrete
